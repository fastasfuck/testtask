# VPC module
```sh
  vpc_name           = var.vpc_name # Name of VPC
  vpc_cidr           = var.vpc_cidr # VPC cidr
  privat_subnets_vpc = var.privat_subnets_vpc # VPC private subnet
  public_subnets_vpc = var.public_subnets_vpc # VPC public subnet
  vpc_azs            = var.vpc_azs # VPC availability zones
```
